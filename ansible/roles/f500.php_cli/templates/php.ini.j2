[PHP]

;;;;;;;;;;;;;;;;;;;
; About php.ini   ;
;;;;;;;;;;;;;;;;;;;
; PHP's initialization file, generally called php.ini, is responsible for
; configuring many of the aspects of PHP's behavior.

; PHP attempts to find and load this configuration from a number of locations.
; The following is a summary of its search order:
; 1. SAPI module specific location.
; 2. The PHPRC environment variable. (As of PHP 5.2.0)
; 3. A number of predefined registry keys on Windows (As of PHP 5.2.0)
; 4. Current working directory (except CLI)
; 5. The web server's directory (for SAPI modules), or directory of PHP
; (otherwise in Windows)
; 6. The directory from the --with-config-file-path compile time option, or the
; Windows directory (C:\windows or C:\winnt)
; See the PHP docs for more specific information.
; http://php.net/configuration.file

; The syntax of the file is extremely simple.  Whitespace and lines
; beginning with a semicolon are silently ignored (as you probably guessed).
; Section headers (e.g. [Foo]) are also silently ignored, even though
; they might mean something in the future.

; Directives following the section heading [PATH=/www/mysite] only
; apply to PHP files in the /www/mysite directory.  Directives
; following the section heading [HOST=www.example.com] only apply to
; PHP files served from www.example.com.  Directives set in these
; special sections cannot be overridden by user-defined INI files or
; at runtime. Currently, [PATH=] and [HOST=] sections only work under
; CGI/FastCGI.
; http://php.net/ini.sections

; Directives are specified using the following syntax:
; directive = value
; Directive names are *case sensitive* - foo=bar is different from FOO=bar.
; Directives are variables used to configure PHP or PHP extensions.
; There is no name validation.  If PHP can't find an expected
; directive because it is not set or is mistyped, a default value will be used.

; The value can be a string, a number, a PHP constant (e.g. E_ALL or M_PI), one
; of the INI constants (On, Off, True, False, Yes, No and None) or an expression
; (e.g. E_ALL & ~E_NOTICE), a quoted string ("bar"), or a reference to a
; previously set variable or directive (e.g. ${foo})

; Expressions in the INI file are limited to bitwise operators and parentheses:
; |  bitwise OR
; ^  bitwise XOR
; &  bitwise AND
; ~  bitwise NOT
; !  boolean NOT

; Boolean flags can be turned on using the values 1, On, True or Yes.
; They can be turned off using the values 0, Off, False or No.

; An empty string can be denoted by simply not writing anything after the equal
; sign, or by using the None keyword:

;  foo =         ; sets foo to an empty string
;  foo = None    ; sets foo to an empty string
;  foo = "None"  ; sets foo to the string 'None'

; If you use constants in your value, and these constants belong to a
; dynamically loaded extension (either a PHP extension or a Zend extension),
; you may only use these constants *after* the line that loads the extension.

;;;;;;;;;;;;;;;;;;;
; About this file ;
;;;;;;;;;;;;;;;;;;;
; PHP comes packaged with two INI files. One that is recommended to be used
; in production environments and one that is recommended to be used in
; development environments.

; php.ini-production contains settings which hold security, performance and
; best practices at its core. But please be aware, these settings may break
; compatibility with older or less security conscience applications. We
; recommending using the production ini in production and testing environments.

; php.ini-development is very similar to its production variant, except it's
; much more verbose when it comes to errors. We recommending using the
; development version only in development environments as errors shown to
; application users can inadvertently leak otherwise secure information.

; This is php.ini-production INI file.

;;;;;;;;;;;;;;;;;;;
; Quick Reference ;
;;;;;;;;;;;;;;;;;;;
; The following are all the settings which are different in either the production
; or development versions of the INIs with respect to PHP's default behavior.
; Please see the actual settings later in the document for more details as to why
; we recommend these changes in PHP's behavior.

; display_errors
;   Default Value: On
;   Development Value: On
;   Production Value: Off

; display_startup_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: Off

; error_reporting
;   Default Value: E_ALL & ~E_NOTICE & ~E_STRICT & ~E_DEPRECATED
;   Development Value: E_ALL
;   Production Value: E_ALL & ~E_DEPRECATED & ~E_STRICT

; html_errors
;   Default Value: On
;   Development Value: On
;   Production value: On

; log_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: On

; max_input_time
;   Default Value: -1 (Unlimited)
;   Development Value: 60 (60 seconds)
;   Production Value: 60 (60 seconds)

; output_buffering
;   Default Value: Off
;   Development Value: 4096
;   Production Value: 4096

; register_argc_argv
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; request_order
;   Default Value: None
;   Development Value: "GP"
;   Production Value: "GP"

; session.gc_divisor
;   Default Value: 100
;   Development Value: 1000
;   Production Value: 1000

; session.hash_bits_per_character
;   Default Value: 4
;   Development Value: 5
;   Production Value: 5

; short_open_tag
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; track_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: Off

; url_rewriter.tags
;   Default Value: "a=href,area=href,frame=src,form=,fieldset="
;   Development Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
;   Production Value: "a=href,area=href,frame=src,input=src,form=fakeentry"

; variables_order
;   Default Value: "EGPCS"
;   Development Value: "GPCS"
;   Production Value: "GPCS"

;;;;;;;;;;;;;;;;;;;;
; php.ini Options  ;
;;;;;;;;;;;;;;;;;;;;
; Name for user-defined php.ini (.htaccess) files. Default is ".user.ini"
;user_ini.filename = ".user.ini"

; To disable this feature set this option to empty value
;user_ini.filename =

; TTL for user-defined php.ini files (time-to-live) in seconds. Default is 300 seconds (5 minutes)
;user_ini.cache_ttl = 300

;;;;;;;;;;;;;;;;;;;;
; Language Options ;
;;;;;;;;;;;;;;;;;;;;

; Enable the PHP scripting language engine under Apache.
; http://php.net/engine
{% if php_cli_engine is defined %}
engine = {{ php_cli_engine }}
{% endif %}

; This directive determines whether or not PHP will recognize code between
; <? and ?> tags as PHP source which should be processed as such. It is
; generally recommended that <?php and ?> should be used and that this feature
; should be disabled, as enabling it may result in issues when generating XML
; documents, however this remains supported for backward compatibility reasons.
; Note that this directive does not control the <?= shorthand tag, which can be
; used regardless of this directive.
; Default Value: On
; Development Value: Off
; Production Value: Off
; http://php.net/short-open-tag
{% if php_cli_short_open_tag is defined %}
short_open_tag = {{ php_cli_short_open_tag }}
{% endif %}

; Allow ASP-style <% %> tags.
; http://php.net/asp-tags
{% if php_cli_asp_tags is defined %}
asp_tags = {{ php_cli_asp_tags }}
{% endif %}

; The number of significant digits displayed in floating point numbers.
; http://php.net/precision
{% if php_cli_precision is defined %}
precision = {{ php_cli_precision }}
{% endif %}

; Output buffering is a mechanism for controlling how much output data
; (excluding headers and cookies) PHP should keep internally before pushing that
; data to the client. If your application's output exceeds this setting, PHP
; will send that data in chunks of roughly the size you specify.
; Turning on this setting and managing its maximum buffer size can yield some
; interesting side-effects depending on your application and web server.
; You may be able to send headers and cookies after you've already sent output
; through print or echo. You also may see performance benefits if your server is
; emitting less packets due to buffered output versus PHP streaming the output
; as it gets it. On production servers, 4096 bytes is a good setting for performance
; reasons.
; Note: Output buffering can also be controlled via Output Buffering Control
;   functions.
; Possible Values:
;   On = Enabled and buffer is unlimited. (Use with caution)
;   Off = Disabled
;   Integer = Enables the buffer and sets its maximum size in bytes.
; Note: This directive is hardcoded to Off for the CLI SAPI
; Default Value: Off
; Development Value: 4096
; Production Value: 4096
; http://php.net/output-buffering
{% if php_cli_output_buffering is defined %}
output_buffering = {{ php_cli_output_buffering }}
{% endif %}

; You can redirect all of the output of your scripts to a function.  For
; example, if you set output_handler to "mb_output_handler", character
; encoding will be transparently converted to the specified encoding.
; Setting any output handler automatically turns on output buffering.
; Note: People who wrote portable scripts should not depend on this ini
;   directive. Instead, explicitly set the output handler using ob_start().
;   Using this ini directive may cause problems unless you know what script
;   is doing.
; Note: You cannot use both "mb_output_handler" with "ob_iconv_handler"
;   and you cannot use both "ob_gzhandler" and "zlib.output_compression".
; Note: output_handler must be empty if this is set 'On' !!!!
;   Instead you must use zlib.output_handler.
; http://php.net/output-handler
{% if php_cli_output_handler is defined %}
output_handler = "{{ php_cli_output_handler }}"
{% endif %}

; Transparent output compression using the zlib library
; Valid values for this option are 'off', 'on', or a specific buffer size
; to be used for compression (default is 4KB)
; Note: Resulting chunk size may vary due to nature of compression. PHP
;   outputs chunks that are few hundreds bytes each as a result of
;   compression. If you prefer a larger chunk size for better
;   performance, enable output_buffering in addition.
; Note: You need to use zlib.output_handler instead of the standard
;   output_handler, or otherwise the output will be corrupted.
; http://php.net/zlib.output-compression
{% if php_cli_zlib_output_compression is defined %}
zlib.output_compression = {{ php_cli_zlib_output_compression }}
{% endif %}

; http://php.net/zlib.output-compression-level
{% if php_cli_zlib_output_compression_level is defined %}
zlib.output_compression_level = {{ php_cli_zlib_output_compression_level }}
{% endif %}

; You cannot specify additional output handlers if zlib.output_compression
; is activated here. This setting does the same as output_handler but in
; a different order.
; http://php.net/zlib.output-handler
{% if php_cli_zlib_output_handler is defined %}
zlib.output_handler = {{ php_cli_zlib_output_handler }}
{% endif %}

; Implicit flush tells PHP to tell the output layer to flush itself
; automatically after every output block.  This is equivalent to calling the
; PHP function flush() after each and every call to print() or echo() and each
; and every HTML block.  Turning this option on has serious performance
; implications and is generally recommended for debugging purposes only.
; http://php.net/implicit-flush
; Note: This directive is hardcoded to On for the CLI SAPI
{% if php_cli_implicit_flush is defined %}
implicit_flush = {{ php_cli_implicit_flush }}
{% endif %}

; The unserialize callback function will be called (with the undefined class'
; name as parameter), if the unserializer finds an undefined class
; which should be instantiated. A warning appears if the specified function is
; not defined, or if the function doesn't include/implement the missing class.
; So only set this entry, if you really want to implement such a
; callback-function.
{% if php_cli_unserialize_callback_func is defined %}
unserialize_callback_func = {{ php_cli_unserialize_callback_func }}
{% endif %}

; When floats & doubles are serialized store serialize_precision significant
; digits after the floating point. The default value ensures that when floats
; are decoded with unserialize, the data will remain the same.
{% if php_cli_serialize_precision is defined %}
serialize_precision = {{ php_cli_serialize_precision }}
{% endif %}

; open_basedir, if set, limits all file operations to the defined directory
; and below.  This directive makes most sense if used in a per-directory
; or per-virtualhost web server configuration file. This directive is
; *NOT* affected by whether Safe Mode is turned On or Off.
; http://php.net/open-basedir
{% if php_cli_open_basedir is defined %}
open_basedir = {{ php_cli_open_basedir }}
{% endif %}

; This directive allows you to disable certain functions for security reasons.
; It receives a comma-delimited list of function names. This directive is
; *NOT* affected by whether Safe Mode is turned On or Off.
; http://php.net/disable-functions
{% if php_cli_disable_functions is defined %}
disable_functions = {{ php_cli_disable_functions }}
{% endif %}

; This directive allows you to disable certain classes for security reasons.
; It receives a comma-delimited list of class names. This directive is
; *NOT* affected by whether Safe Mode is turned On or Off.
; http://php.net/disable-classes
{% if php_cli_disable_classes is defined %}
disable_classes = {{ php_cli_disable_classes }}
{% endif %}

; Colors for Syntax Highlighting mode.  Anything that's acceptable in
; <span style="color: ???????"> would work.
; http://php.net/syntax-highlighting
{% if php_cli_highlight_string is defined %}
highlight.string = {{ php_cli_highlight_string }}
{% endif %}
{% if php_cli_highlight_comment is defined %}
highlight.comment = {{ php_cli_highlight_comment }}
{% endif %}
{% if php_cli_highlight_keyword is defined %}
highlight.keyword = {{ php_cli_highlight_keyword }}
{% endif %}
{% if php_cli_highlight_default is defined %}
highlight.default = {{ php_cli_highlight_default }}
{% endif %}
{% if php_cli_highlight_html is defined %}
highlight.html = {{ php_cli_highlight_html }}
{% endif %}

; If enabled, the request will be allowed to complete even if the user aborts
; the request. Consider enabling it if executing long requests, which may end up
; being interrupted by the user or a browser timing out. PHP's default behavior
; is to disable this feature.
; http://php.net/ignore-user-abort
{% if php_cli_ignore_user_abort is defined %}
ignore_user_abort = {{ php_cli_ignore_user_abort }}
{% endif %}

; Determines the size of the realpath cache to be used by PHP. This value should
; be increased on systems where PHP opens many files to reflect the quantity of
; the file operations performed.
; http://php.net/realpath-cache-size
{% if php_cli_realpath_cache_size is defined %}
realpath_cache_size = {{ php_cli_realpath_cache_size }}
{% endif %}

; Duration of time, in seconds for which to cache realpath information for a given
; file or directory. For systems with rarely changing files, consider increasing this
; value.
; http://php.net/realpath-cache-ttl
{% if php_cli_realpath_cache_ttl is defined %}
realpath_cache_ttl = {{ php_cli_realpath_cache_ttl }}
{% endif %}

; Enables or disables the circular reference collector.
; http://php.net/zend.enable-gc
{% if php_cli_zend_enable_gc is defined %}
zend.enable_gc = {{ php_cli_zend_enable_gc }}
{% endif %}

; If enabled, scripts may be written in encodings that are incompatible with
; the scanner.  CP936, Big5, CP949 and Shift_JIS are the examples of such
; encodings.  To use this feature, mbstring extension must be enabled.
; Default: Off
{% if php_cli_zend_multibyte is defined %}
zend.multibyte = {{ php_cli_zend_multibyte }}
{% endif %}

; Allows to set the default encoding for the scripts.  This value will be used
; unless "declare(encoding=...)" directive appears at the top of the script.
; Only affects if zend.multibyte is set.
; Default: ""
{% if php_cli_zend_script_encoding is defined %}
zend.script_encoding = {{ php_cli_zend_script_encoding }}
{% endif %}

;;;;;;;;;;;;;;;;;
; Miscellaneous ;
;;;;;;;;;;;;;;;;;

; Decides whether PHP may expose the fact that it is installed on the server
; (e.g. by adding its signature to the Web server header).  It is no security
; threat in any way, but it makes it possible to determine whether you use PHP
; on your server or not.
; http://php.net/expose-php
{% if php_cli_expose_php is defined %}
expose_php = {{ php_cli_expose_php }}
{% endif %}

;;;;;;;;;;;;;;;;;;;
; Resource Limits ;
;;;;;;;;;;;;;;;;;;;

; Maximum execution time of each script, in seconds
; http://php.net/max-execution-time
; Note: This directive is hardcoded to 0 for the CLI SAPI
{% if php_cli_max_execution_time is defined %}
max_execution_time = {{ php_cli_max_execution_time }}
{% endif %}

; Maximum amount of time each script may spend parsing request data. It's a good
; idea to limit this time on productions servers in order to eliminate unexpectedly
; long running scripts.
; Note: This directive is hardcoded to -1 for the CLI SAPI
; Default Value: -1 (Unlimited)
; Development Value: 60 (60 seconds)
; Production Value: 60 (60 seconds)
; http://php.net/max-input-time
{% if php_cli_max_input_time is defined %}
max_input_time = {{ php_cli_max_input_time }}
{% endif %}

; Maximum input variable nesting level
; http://php.net/max-input-nesting-level
{% if php_cli_max_input_nesting_level is defined %}
max_input_nesting_level = {{ php_cli_max_input_nesting_level }}
{% endif %}

; How many GET/POST/COOKIE input variables may be accepted
{% if php_cli_max_input_vars is defined %}
max_input_vars = {{ php_cli_max_input_vars }}
{% endif %}

; Maximum amount of memory a script may consume (128MB)
; http://php.net/memory-limit
{% if php_cli_memory_limit is defined %}
memory_limit = {{ php_cli_memory_limit }}
{% endif %}

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; Error handling and logging ;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

; This directive informs PHP of which errors, warnings and notices you would like
; it to take action for. The recommended way of setting values for this
; directive is through the use of the error level constants and bitwise
; operators. The error level constants are below here for convenience as well as
; some common settings and their meanings.
; By default, PHP is set to take action on all errors, notices and warnings EXCEPT
; those related to E_NOTICE and E_STRICT, which together cover best practices and
; recommended coding standards in PHP. For performance reasons, this is the
; recommend error reporting setting. Your production server shouldn't be wasting
; resources complaining about best practices and coding standards. That's what
; development servers and development settings are for.
; Note: The php.ini-development file has this setting as E_ALL. This
; means it pretty much reports everything which is exactly what you want during
; development and early testing.
;
; Error Level Constants:
; E_ALL             - All errors and warnings (includes E_STRICT as of PHP 5.4.0)
; E_ERROR           - fatal run-time errors
; E_RECOVERABLE_ERROR  - almost fatal run-time errors
; E_WARNING         - run-time warnings (non-fatal errors)
; E_PARSE           - compile-time parse errors
; E_NOTICE          - run-time notices (these are warnings which often result
;                     from a bug in your code, but it's possible that it was
;                     intentional (e.g., using an uninitialized variable and
;                     relying on the fact it's automatically initialized to an
;                     empty string)
; E_STRICT          - run-time notices, enable to have PHP suggest changes
;                     to your code which will ensure the best interoperability
;                     and forward compatibility of your code
; E_CORE_ERROR      - fatal errors that occur during PHP's initial startup
; E_CORE_WARNING    - warnings (non-fatal errors) that occur during PHP's
;                     initial startup
; E_COMPILE_ERROR   - fatal compile-time errors
; E_COMPILE_WARNING - compile-time warnings (non-fatal errors)
; E_USER_ERROR      - user-generated error message
; E_USER_WARNING    - user-generated warning message
; E_USER_NOTICE     - user-generated notice message
; E_DEPRECATED      - warn about code that will not work in future versions
;                     of PHP
; E_USER_DEPRECATED - user-generated deprecation warnings
;
; Common Values:
;   E_ALL (Show all errors, warnings and notices including coding standards.)
;   E_ALL & ~E_NOTICE  (Show all errors, except for notices)
;   E_ALL & ~E_NOTICE & ~E_STRICT  (Show all errors, except for notices and coding standards warnings.)
;   E_COMPILE_ERROR|E_RECOVERABLE_ERROR|E_ERROR|E_CORE_ERROR  (Show only errors)
; Default Value: E_ALL & ~E_NOTICE & ~E_STRICT & ~E_DEPRECATED
; Development Value: E_ALL
; Production Value: E_ALL & ~E_DEPRECATED & ~E_STRICT
; http://php.net/error-reporting
{% if php_cli_error_reporting is defined %}
error_reporting = {{ php_cli_error_reporting }}
{% endif %}

; This directive controls whether or not and where PHP will output errors,
; notices and warnings too. Error output is very useful during development, but
; it could be very dangerous in production environments. Depending on the code
; which is triggering the error, sensitive information could potentially leak
; out of your application such as database usernames and passwords or worse.
; It's recommended that errors be logged on production servers rather than
; having the errors sent to STDOUT.
; Possible Values:
;   Off = Do not display any errors
;   stderr = Display errors to STDERR (affects only CGI/CLI binaries!)
;   On or stdout = Display errors to STDOUT
; Default Value: On
; Development Value: On
; Production Value: Off
; http://php.net/display-errors
{% if php_cli_display_errors is defined %}
display_errors = {{ php_cli_display_errors }}
{% endif %}

; The display of errors which occur during PHP's startup sequence are handled
; separately from display_errors. PHP's default behavior is to suppress those
; errors from clients. Turning the display of startup errors on can be useful in
; debugging configuration problems. But, it's strongly recommended that you
; leave this setting off on production servers.
; Default Value: Off
; Development Value: On
; Production Value: Off
; http://php.net/display-startup-errors
{% if php_cli_display_startup_errors is defined %}
display_startup_errors = {{ php_cli_display_startup_errors }}
{% endif %}

; Besides displaying errors, PHP can also log errors to locations such as a
; server-specific log, STDERR, or a location specified by the error_log
; directive found below. While errors should not be displayed on productions
; servers they should still be monitored and logging is a great way to do that.
; Default Value: Off
; Development Value: On
; Production Value: On
; http://php.net/log-errors
{% if php_cli_log_errors is defined %}
log_errors = {{ php_cli_log_errors }}
{% endif %}

; Set maximum length of log_errors. In error_log information about the source is
; added. The default is 1024 and 0 allows to not apply any maximum length at all.
; http://php.net/log-errors-max-len
{% if php_cli_log_errors_max_len is defined %}
log_errors_max_len = {{ php_cli_log_errors_max_len }}
{% endif %}

; Do not log repeated messages. Repeated errors must occur in same file on same
; line unless ignore_repeated_source is set true.
; http://php.net/ignore-repeated-errors
{% if php_cli_ignore_repeated_errors is defined %}
ignore_repeated_errors = {{ php_cli_ignore_repeated_errors }}
{% endif %}

; Ignore source of message when ignoring repeated messages. When this setting
; is On you will not log errors with repeated messages from different files or
; source lines.
; http://php.net/ignore-repeated-source
{% if php_cli_ignore_repeated_source is defined %}
ignore_repeated_source = {{ php_cli_ignore_repeated_source }}
{% endif %}

; If this parameter is set to Off, then memory leaks will not be shown (on
; stdout or in the log). This has only effect in a debug compile, and if
; error reporting includes E_WARNING in the allowed list
; http://php.net/report-memleaks
{% if php_cli_report_memleaks is defined %}
report_memleaks = {{ php_cli_report_memleaks }}
{% endif %}

; This setting is on by default.
{% if php_cli_report_zend_debug is defined %}
report_zend_debug = {{ php_cli_report_zend_debug }}
{% endif %}

; Store the last error/warning message in $php_errormsg (boolean). Setting this value
; to On can assist in debugging and is appropriate for development servers. It should
; however be disabled on production servers.
; Default Value: Off
; Development Value: On
; Production Value: Off
; http://php.net/track-errors
{% if php_cli_track_errors is defined %}
track_errors = {{ php_cli_track_errors }}
{% endif %}

; Turn off normal error reporting and emit XML-RPC error XML
; http://php.net/xmlrpc-errors
{% if php_cli_xmlrpc_errors is defined %}
xmlrpc_errors = {{ php_cli_xmlrpc_errors }}
{% endif %}

; An XML-RPC faultCode
{% if php_cli_xmlrpc_error_number is defined %}
xmlrpc_error_number = {{ php_cli_xmlrpc_error_number }}
{% endif %}

; When PHP displays or logs an error, it has the capability of formatting the
; error message as HTML for easier reading. This directive controls whether
; the error message is formatted as HTML or not.
; Note: This directive is hardcoded to Off for the CLI SAPI
; Default Value: On
; Development Value: On
; Production value: On
; http://php.net/html-errors
{% if php_cli_html_errors is defined %}
html_errors = {{ php_cli_html_errors }}
{% endif %}

; If html_errors is set to On *and* docref_root is not empty, then PHP
; produces clickable error messages that direct to a page describing the error
; or function causing the error in detail.
; You can download a copy of the PHP manual from http://php.net/docs
; and change docref_root to the base URL of your local copy including the
; leading '/'. You must also specify the file extension being used including
; the dot. PHP's default behavior is to leave these settings empty, in which
; case no links to documentation are generated.
; Note: Never use this feature for production boxes.
; http://php.net/docref-root
; Examples
{% if php_cli_docref_root is defined %}
docref_root = {{ php_cli_docref_root }}
{% endif %}

; http://php.net/docref-ext
{% if php_cli_docref_ext is defined %}
docref_ext = {{ php_cli_docref_ext }}
{% endif %}

; String to output before an error message. PHP's default behavior is to leave
; this setting blank.
; http://php.net/error-prepend-string
; Example:
{% if php_cli_error_prepend_string is defined %}
error_prepend_string = {{ php_cli_error_prepend_string }}
{% endif %}

; String to output after an error message. PHP's default behavior is to leave
; this setting blank.
; http://php.net/error-append-string
; Example:
{% if php_cli_error_append_string is defined %}
error_append_string = {{ php_cli_error_append_string }}
{% endif %}

; Log errors to specified file. PHP's default behavior is to leave this value
; empty.
; http://php.net/error-log
; Example:
;error_log = php_errors.log
; Log errors to syslog (Event Log on NT, not valid in Windows 95).
;error_log = syslog
{% if php_cli_error_log is defined %}
error_log = {{ php_cli_error_log }}
{% endif %}

;windows.show_crt_warning
; Default value: 0
; Development value: 0
; Production value: 0
{% if php_cli_windows_show_crt_warning is defined %}
windows.show_crt_warning = {{ php_cli_windows_show_crt_warning }}
{% endif %}

;;;;;;;;;;;;;;;;;
; Data Handling ;
;;;;;;;;;;;;;;;;;

; The separator used in PHP generated URLs to separate arguments.
; PHP's default setting is "&".
; http://php.net/arg-separator.output
; Example:
{% if php_cli_arg_separator_output is defined %}
arg_separator.output = {{ php_cli_arg_separator_output }}
{% endif %}

; List of separator(s) used by PHP to parse input URLs into variables.
; PHP's default setting is "&".
; NOTE: Every character in this directive is considered as separator!
; http://php.net/arg-separator.input
; Example:
{% if php_cli_arg_separator_input is defined %}
arg_separator.input = {{ php_cli_arg_separator_input }}
{% endif %}

; This directive determines which super global arrays are registered when PHP
; starts up. G,P,C,E & S are abbreviations for the following respective super
; globals: GET, POST, COOKIE, ENV and SERVER. There is a performance penalty
; paid for the registration of these arrays and because ENV is not as commonly
; used as the others, ENV is not recommended on productions servers. You
; can still get access to the environment variables through getenv() should you
; need to.
; Default Value: "EGPCS"
; Development Value: "GPCS"
; Production Value: "GPCS";
; http://php.net/variables-order
{% if php_cli_variables_order is defined %}
variables_order = {{ php_cli_variables_order }}
{% endif %}

; This directive determines which super global data (G,P,C,E & S) should
; be registered into the super global array REQUEST. If so, it also determines
; the order in which that data is registered. The values for this directive are
; specified in the same manner as the variables_order directive, EXCEPT one.
; Leaving this value empty will cause PHP to use the value set in the
; variables_order directive. It does not mean it will leave the super globals
; array REQUEST empty.
; Default Value: None
; Development Value: "GP"
; Production Value: "GP"
; http://php.net/request-order
{% if php_cli_request_order is defined %}
request_order = {{ php_cli_request_order }}
{% endif %}

; This directive determines whether PHP registers $argv & $argc each time it
; runs. $argv contains an array of all the arguments passed to PHP when a script
; is invoked. $argc contains an integer representing the number of arguments
; that were passed when the script was invoked. These arrays are extremely
; useful when running scripts from the command line. When this directive is
; enabled, registering these variables consumes CPU cycles and memory each time
; a script is executed. For performance reasons, this feature should be disabled
; on production servers.
; Note: This directive is hardcoded to On for the CLI SAPI
; Default Value: On
; Development Value: Off
; Production Value: Off
; http://php.net/register-argc-argv
{% if php_cli_register_argc_argv is defined %}
register_argc_argv = {{ php_cli_register_argc_argv }}
{% endif %}

; When enabled, the ENV, REQUEST and SERVER variables are created when they're
; first used (Just In Time) instead of when the script starts. If these
; variables are not used within a script, having this directive on will result
; in a performance gain. The PHP directive register_argc_argv must be disabled
; for this directive to have any affect.
; http://php.net/auto-globals-jit
{% if php_cli_auto_globals_jit is defined %}
auto_globals_jit = {{ php_cli_auto_globals_jit }}
{% endif %}

; Whether PHP will read the POST data.
; This option is enabled by default.
; Most likely, you won't want to disable this option globally. It causes $_POST
; and $_FILES to always be empty; the only way you will be able to read the
; POST data will be through the php://input stream wrapper. This can be useful
; to proxy requests or to process the POST data in a memory efficient fashion.
; http://php.net/enable-post-data-reading
{% if php_cli_enable_post_data_reading is defined %}
enable_post_data_reading = {{ php_cli_enable_post_data_reading }}
{% endif %}

; Maximum size of POST data that PHP will accept.
; Its value may be 0 to disable the limit. It is ignored if POST data reading
; is disabled through enable_post_data_reading.
; http://php.net/post-max-size
{% if php_cli_post_max_size is defined %}
post_max_size = {{ php_cli_post_max_size }}
{% endif %}

; Automatically add files before PHP document.
; http://php.net/auto-prepend-file
{% if php_cli_auto_prepend_file is defined %}
auto_prepend_file = {{ php_cli_auto_prepend_file }}
{% endif %}

; Automatically add files after PHP document.
; http://php.net/auto-append-file
{% if php_cli_auto_append_file is defined %}
auto_append_file = {{ php_cli_auto_append_file }}
{% endif %}

; By default, PHP will output a character encoding using
; the Content-type: header.  To disable sending of the charset, simply
; set it to be empty.
;
; PHP's built-in default is text/html
; http://php.net/default-mimetype
{% if php_cli_default_mimetype is defined %}
default_mimetype = {{ php_cli_default_mimetype }}
{% endif %}

; PHP's default character set is set to empty.
; http://php.net/default-charset
{% if php_cli_default_charset is defined %}
default_charset = {{ php_cli_default_charset }}
{% endif %}

; Always populate the $HTTP_RAW_POST_DATA variable. PHP's default behavior is
; to disable this feature. If post reading is disabled through
; enable_post_data_reading, $HTTP_RAW_POST_DATA is *NOT* populated.
; http://php.net/always-populate-raw-post-data
{% if php_cli_always_populate_raw_post_data is defined %}
always_populate_raw_post_data = {{ php_cli_always_populate_raw_post_data }}
{% endif %}

;;;;;;;;;;;;;;;;;;;;;;;;;
; Paths and Directories ;
;;;;;;;;;;;;;;;;;;;;;;;;;

; UNIX: "/path1:/path2"
;include_path = ".:/php/includes"
;
; Windows: "\path1;\path2"
;include_path = ".;c:\php\includes"
;
; PHP's default setting for include_path is ".;/path/to/php/pear"
; http://php.net/include-path
{% if php_cli_include_path is defined %}
include_path = {{ php_cli_include_path }}
{% endif %}

; The root of the PHP pages, used only if nonempty.
; if PHP was not compiled with FORCE_REDIRECT, you SHOULD set doc_root
; if you are running php as a CGI under any web server (other than IIS)
; see documentation for security issues.  The alternate is to use the
; cgi.force_redirect configuration below
; http://php.net/doc-root
{% if php_cli_doc_root is defined %}
doc_root = {{ php_cli_doc_root }}
{% endif %}

; The directory under which PHP opens the script using /~username used only
; if nonempty.
; http://php.net/user-dir
{% if php_cli_user_dir is defined %}
user_dir = {{ php_cli_user_dir }}
{% endif %}

; Directory in which the loadable extensions (modules) reside.
; http://php.net/extension-dir
; extension_dir = "./"
; On windows:
; extension_dir = "ext"
{% if php_cli_extension_dir is defined %}
extension_dir = {{ php_cli_extension_dir }}
{% endif %}

; Directory where the temporary files should be placed.
; Defaults to the system default (see sys_get_temp_dir)
{% if php_cli_sys_temp_dir is defined %}
sys_temp_dir = {{ php_cli_sys_temp_dir }}
{% endif %}

; Whether or not to enable the dl() function.  The dl() function does NOT work
; properly in multithreaded servers, such as IIS or Zeus, and is automatically
; disabled on them.
; http://php.net/enable-dl
{% if php_cli_enable_dl is defined %}
enable_dl = {{ php_cli_enable_dl }}
{% endif %}

; cgi.force_redirect is necessary to provide security running PHP as a CGI under
; most web servers.  Left undefined, PHP turns this on by default.  You can
; turn it off here AT YOUR OWN RISK
; **You CAN safely turn this off for IIS, in fact, you MUST.**
; http://php.net/cgi.force-redirect
{% if php_cli_cgi_force_redirect is defined %}
cgi.force_redirect = {{ php_cli_cgi_force_redirect }}
{% endif %}

; if cgi.nph is enabled it will force cgi to always sent Status: 200 with
; every request. PHP's default behavior is to disable this feature.
{% if php_cli_cgi_nph is defined %}
cgi.nph = {{ php_cli_cgi_nph }}
{% endif %}

; if cgi.force_redirect is turned on, and you are not running under Apache or Netscape
; (iPlanet) web servers, you MAY need to set an environment variable name that PHP
; will look for to know it is OK to continue execution.  Setting this variable MAY
; cause security issues, KNOW WHAT YOU ARE DOING FIRST.
; http://php.net/cgi.redirect-status-env
{% if php_cli_cgi_redirect_status_env is defined %}
cgi.redirect_status_env = {{ php_cli_cgi_redirect_status_env }}
{% endif %}

; cgi.fix_pathinfo provides *real* PATH_INFO/PATH_TRANSLATED support for CGI.  PHP's
; previous behaviour was to set PATH_TRANSLATED to SCRIPT_FILENAME, and to not grok
; what PATH_INFO is.  For more information on PATH_INFO, see the cgi specs.  Setting
; this to 1 will cause PHP CGI to fix its paths to conform to the spec.  A setting
; of zero causes PHP to behave as before.  Default is 1.  You should fix your scripts
; to use SCRIPT_FILENAME rather than PATH_TRANSLATED.
; http://php.net/cgi.fix-pathinfo
{% if php_cli_cgi_fix_pathinfo is defined %}
cgi.fix_pathinfo = {{ php_cli_cgi_fix_pathinfo }}
{% endif %}

; FastCGI under IIS (on WINNT based OS) supports the ability to impersonate
; security tokens of the calling client.  This allows IIS to define the
; security context that the request runs under.  mod_fastcgi under Apache
; does not currently support this feature (03/17/2002)
; Set to 1 if running under IIS.  Default is zero.
; http://php.net/fastcgi.impersonate
{% if php_cli_fastcgi_impersonate is defined %}
fastcgi.impersonate = {{ php_cli_fastcgi_impersonate }}
{% endif %}

; Disable logging through FastCGI connection. PHP's default behavior is to enable
; this feature.
{% if php_cli_fastcgi_logging is defined %}
fastcgi.logging = {{ php_cli_fastcgi_logging }}
{% endif %}

; cgi.rfc2616_headers configuration option tells PHP what type of headers to
; use when sending HTTP response code. If it's set 0 PHP sends Status: header that
; is supported by Apache. When this option is set to 1 PHP will send
; RFC2616 compliant header.
; Default is zero.
; http://php.net/cgi.rfc2616-headers
{% if php_cli_cgi_rfc2616_headers is defined %}
cgi.rfc2616_headers = {{ php_cli_cgi_rfc2616_headers }}
{% endif %}

;;;;;;;;;;;;;;;;
; File Uploads ;
;;;;;;;;;;;;;;;;

; Whether to allow HTTP file uploads.
; http://php.net/file-uploads
{% if php_cli_file_uploads is defined %}
file_uploads = {{ php_cli_file_uploads }}
{% endif %}

; Temporary directory for HTTP uploaded files (will use system default if not
; specified).
; http://php.net/upload-tmp-dir
{% if php_cli_upload_tmp_dir is defined %}
upload_tmp_dir = {{ php_cli_upload_tmp_dir }}
{% endif %}

; Maximum allowed size for uploaded files.
; http://php.net/upload-max-filesize
{% if php_cli_upload_max_filesize is defined %}
upload_max_filesize = {{ php_cli_upload_max_filesize }}
{% endif %}

; Maximum number of files that can be uploaded via a single request
{% if php_cli_max_file_uploads is defined %}
max_file_uploads = {{ php_cli_max_file_uploads }}
{% endif %}

;;;;;;;;;;;;;;;;;;
; Fopen wrappers ;
;;;;;;;;;;;;;;;;;;

; Whether to allow the treatment of URLs (like http:// or ftp://) as files.
; http://php.net/allow-url-fopen
{% if php_cli_allow_url_fopen is defined %}
allow_url_fopen = {{ php_cli_allow_url_fopen }}
{% endif %}

; Whether to allow include/require to open URLs (like http:// or ftp://) as files.
; http://php.net/allow-url-include
{% if php_cli_allow_url_include is defined %}
allow_url_include = {{ php_cli_allow_url_include }}
{% endif %}

; Define the anonymous ftp password (your email address). PHP's default setting
; for this is empty.
; http://php.net/from
{% if php_cli_from is defined %}
from = {{ php_cli_from }}
{% endif %}

; Define the User-Agent string. PHP's default setting for this is empty.
; http://php.net/user-agent
{% if php_cli_user_agent is defined %}
user_agent = {{ php_cli_user_agent }}
{% endif %}

; Default timeout for socket based streams (seconds)
; http://php.net/default-socket-timeout
{% if php_cli_default_socket_timeout is defined %}
default_socket_timeout = {{ php_cli_default_socket_timeout }}
{% endif %}

; If your scripts have to deal with files from Macintosh systems,
; or you are running on a Mac and need to deal with files from
; unix or win32 systems, setting this flag will cause PHP to
; automatically detect the EOL character in those files so that
; fgets() and file() will work regardless of the source of the file.
; http://php.net/auto-detect-line-endings
{% if php_cli_auto_detect_line_endings is defined %}
auto_detect_line_endings = {{ php_cli_auto_detect_line_endings }}
{% endif %}

;;;;;;;;;;;;;;;;;;;;;;
; Dynamic Extensions ;
;;;;;;;;;;;;;;;;;;;;;;

; If you wish to have an extension loaded automatically, use the following
; syntax:
;
;   extension=modulename.extension
;
; For example, on Windows:
;
;   extension=msql.dll
;
; ... or under UNIX:
;
;   extension=msql.so
;
; ... or with a path:
;
;   extension=/path/to/extension/msql.so
;
; If you only provide the name of the extension, PHP will look for it in its
; default extension directory.
;
; Windows Extensions
; Note that ODBC support is built in, so no dll is needed for it.
; Note that many DLL files are located in the extensions/ (PHP 4) ext/ (PHP 5)
; extension folders as well as the separate PECL DLL download (PHP 5).
; Be sure to appropriately set the extension_dir directive.
;
;extension=php_bz2.dll
;extension=php_curl.dll
;extension=php_fileinfo.dll
;extension=php_gd2.dll
;extension=php_gettext.dll
;extension=php_gmp.dll
;extension=php_intl.dll
;extension=php_imap.dll
;extension=php_interbase.dll
;extension=php_ldap.dll
;extension=php_mbstring.dll
;extension=php_exif.dll      ; Must be after mbstring as it depends on it
;extension=php_mysql.dll
;extension=php_mysqli.dll
;extension=php_oci8.dll      ; Use with Oracle 10gR2 Instant Client
;extension=php_oci8_11g.dll  ; Use with Oracle 11gR2 Instant Client
;extension=php_openssl.dll
;extension=php_pdo_firebird.dll
;extension=php_pdo_mysql.dll
;extension=php_pdo_oci.dll
;extension=php_pdo_odbc.dll
;extension=php_pdo_pgsql.dll
;extension=php_pdo_sqlite.dll
;extension=php_pgsql.dll
;extension=php_pspell.dll
;extension=php_shmop.dll

; The MIBS data available in the PHP distribution must be installed.
; See http://www.php.net/manual/en/snmp.installation.php
;extension=php_snmp.dll

;extension=php_soap.dll
;extension=php_sockets.dll
;extension=php_sqlite3.dll
;extension=php_sybase_ct.dll
;extension=php_tidy.dll
;extension=php_xmlrpc.dll
;extension=php_xsl.dll

{% if php_cli_extensions is defined %}
{% for extension in php_extensions %}
extension = {{ extension }}
{% endfor %}
{% endif %}

;;;;;;;;;;;;;;;;;;;
; Module Settings ;
;;;;;;;;;;;;;;;;;;;

[CLI Server]
; Whether the CLI web server uses ANSI color coding in its terminal output.
{% if php_cli_cli_server_color is defined %}
cli_server.color = {{ php_cli_cli_server_color }}
{% endif %}

[Date]
; Defines the default timezone used by the date functions
; http://php.net/date.timezone
{% if php_cli_date_timezone is defined %}
date.timezone = {{ php_cli_date_timezone }}
{% endif %}

; http://php.net/date.default-latitude
{% if php_cli_date_default_latitude is defined %}
date.default_latitude = {{ php_cli_date_default_latitude }}
{% endif %}

; http://php.net/date.default-longitude
{% if php_cli_date_default_longitude is defined %}
date.default_longitude = {{ php_cli_date_default_longitude }}
{% endif %}

; http://php.net/date.sunrise-zenith
{% if php_cli_date_sunrise_zenith is defined %}
date.sunrise_zenith = {{ php_cli_date_sunrise_zenith }}
{% endif %}

; http://php.net/date.sunset-zenith
{% if php_cli_date_sunset_zenith is defined %}
date.sunset_zenith = {{ php_cli_date_sunset_zenith }}
{% endif %}

[filter]
; http://php.net/filter.default
{% if php_cli_filter_default is defined %}
filter.default = {{ php_cli_filter_default }}
{% endif %}

; http://php.net/filter.default-flags
{% if php_cli_filter_default_flags is defined %}
filter.default_flags = {{ php_cli_filter_default_flags }}
{% endif %}

[iconv]
{% if php_cli_iconv_input_encoding is defined %}
iconv.input_encoding = {{ php_cli_iconv_input_encoding }}
{% endif %}
{% if php_cli_iconv_internal_encoding is defined %}
iconv.internal_encoding = {{ php_cli_iconv_internal_encoding }}
{% endif %}
{% if php_cli_iconv_output_encoding is defined %}
iconv.output_encoding = {{ php_cli_iconv_output_encoding }}
{% endif %}

[intl]
{% if php_cli_intl_default_locale is defined %}
intl.default_locale = {{ php_cli_intl_default_locale }}
{% endif %}

; This directive allows you to produce PHP errors when some error
; happens within intl functions. The value is the level of the error produced.
; Default is 0, which does not produce any errors.
{% if php_cli_intl_error_level is defined %}
intl.error_level = {{ php_cli_intl_error_level }}
{% endif %}

[sqlite]
; http://php.net/sqlite.assoc-case
{% if php_cli_sqlite_assoc_case is defined %}
sqlite.assoc_case = {{ php_cli_sqlite_assoc_case }}
{% endif %}

[sqlite3]
{% if php_cli_sqlite3_extension_dir is defined %}
sqlite3.extension_dir = {{ php_cli_sqlite3_extension_dir }}
{% endif %}

[Pcre]
;PCRE library backtracking limit.
; http://php.net/pcre.backtrack-limit
{% if php_cli_pcre_backtrack_limit is defined %}
pcre.backtrack_limit = {{ php_cli_pcre_backtrack_limit }}
{% endif %}

;PCRE library recursion limit.
;Please note that if you set this value to a high number you may consume all
;the available process stack and eventually crash PHP (due to reaching the
;stack size limit imposed by the Operating System).
; http://php.net/pcre.recursion-limit
{% if php_cli_pcre_recursion_limit is defined %}
pcre.recursion_limit = {{ php_cli_pcre_recursion_limit }}
{% endif %}

[Pdo]
; Whether to pool ODBC connections. Can be one of "strict", "relaxed" or "off"
; http://php.net/pdo-odbc.connection-pooling
{% if php_cli_pdo_odbc_connection_pooling is defined %}
pdo_odbc.connection_pooling = {{ php_cli_pdo_odbc_connection_pooling }}
{% endif %}

{% if php_cli_pdo_odbc_db2_instance_name is defined %}
pdo_odbc.db2_instance_name = {{ php_cli_pdo_odbc_db2_instance_name }}
{% endif %}

[Pdo_mysql]
; If mysqlnd is used: Number of cache slots for the internal result set cache
; http://php.net/pdo_mysql.cache_size
{% if php_cli_pdo_mysql_cache_size is defined %}
pdo_mysql.cache_size = {{ php_cli_pdo_mysql_cache_size }}
{% endif %}

; Default socket name for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/pdo_mysql.default-socket
{% if php_cli_pdo_mysql_default_socket is defined %}
pdo_mysql.default_socket = {{ php_cli_pdo_mysql_default_socket }}
{% endif %}

[Phar]
; http://php.net/phar.readonly
{% if php_cli_phar_readonly is defined %}
phar.readonly = {{ php_cli_phar_readonly }}
{% endif %}

; http://php.net/phar.require-hash
{% if php_cli_phar_require_hash is defined %}
phar.require_hash = {{ php_cli_phar_require_hash }}
{% endif %}

{% if php_cli_phar_cache_list is defined %}
phar.cache_list = {{ php_cli_phar_cache_list }}
{% endif %}

[mail function]
; For Win32 only.
; http://php.net/smtp
{% if php_cli_mail_smtp_host is defined %}
SMTP = {{ php_cli_mail_smtp_host }}
{% endif %}

; http://php.net/smtp-port
{% if php_cli_mail_smtp_port is defined %}
smtp_port = {{ php_cli_mail_smtp_port }}
{% endif %}

; For Win32 only.
; http://php.net/sendmail-from
{% if php_cli_mail_sendmail_from is defined %}
sendmail_from = {{ php_cli_mail_sendmail_from }}
{% endif %}

; For Unix only.  You may supply arguments as well (default: "sendmail -t -i").
; http://php.net/sendmail-path
{% if php_cli_mail_sendmail_path is defined %}
sendmail_path = {{ php_cli_mail_sendmail_path }}
{% endif %}

; Force the addition of the specified parameters to be passed as extra parameters
; to the sendmail binary. These parameters will always replace the value of
; the 5th parameter to mail(), even in safe mode.
{% if php_cli_mail_force_extra_parameters is defined %}
mail.force_extra_parameters = {{ php_cli_mail_force_extra_parameters }}
{% endif %}

; Add X-PHP-Originating-Script: that will include uid of the script followed by the filename
{% if php_cli_mail_add_x_header is defined %}
mail.add_x_header = {{ php_cli_mail_add_x_header }}
{% endif %}

; The path to a log file that will log all mail() calls. Log entries include
; the full path of the script, line number, To address and headers.
;mail.log =
; Log mail to syslog (Event Log on NT, not valid in Windows 95).
;mail.log = syslog
{% if php_cli_mail_log is defined %}
mail.log = {{ php_cli_mail_log }}
{% endif %}

[SQL]
; http://php.net/sql.safe-mode
{% if php_cli_sql_safe_mode is defined %}
sql.safe_mode = {{ php_cli_sql_safe_mode }}
{% endif %}

[ODBC]
; http://php.net/odbc.default-db
{% if php_cli_odbc_default_db is defined %}
odbc.default_db = {{ php_cli_odbc_default_db }}
{% endif %}

; http://php.net/odbc.default-user
{% if php_cli_odbc_default_user is defined %}
odbc.default_user = {{ php_cli_odbc_default_user }}
{% endif %}

; http://php.net/odbc.default-pw
{% if php_cli_odbc_default_pw is defined %}
odbc.default_pw = {{ php_cli_odbc_default_pw }}
{% endif %}

; Controls the ODBC cursor model.
; Default: SQL_CURSOR_STATIC (default).
{% if php_cli_odbc_default_cursortype is defined %}
odbc.default_cursortype = {{ php_cli_odbc_default_cursortype }}
{% endif %}

; Allow or prevent persistent links.
; http://php.net/odbc.allow-persistent
{% if php_cli_odbc_allow_persistent is defined %}
odbc.allow_persistent = {{ php_cli_odbc_allow_persistent }}
{% endif %}

; Check that a connection is still valid before reuse.
; http://php.net/odbc.check-persistent
{% if php_cli_odbc_check_persistent is defined %}
odbc.check_persistent = {{ php_cli_odbc_check_persistent }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/odbc.max-persistent
{% if php_cli_odbc_max_persistent is defined %}
odbc.max_persistent = {{ php_cli_odbc_max_persistent }}
{% endif %}

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/odbc.max-links
{% if php_cli_odbc_max_links is defined %}
odbc.max_links = {{ php_cli_odbc_max_links }}
{% endif %}

; Handling of LONG fields.  Returns number of bytes to variables.  0 means
; passthru.
; http://php.net/odbc.defaultlrl
{% if php_cli_odbc_default_lrl is defined %}
odbc.defaultlrl = {{ php_cli_odbc_default_lrl }}
{% endif %}

; Handling of binary data.  0 means passthru, 1 return as is, 2 convert to char.
; See the documentation on odbc_binmode and odbc_longreadlen for an explanation
; of odbc.defaultlrl and odbc.defaultbinmode
; http://php.net/odbc.defaultbinmode
{% if php_cli_odbc_default_binmode is defined %}
odbc.defaultbinmode = {{ php_cli_odbc_default_binmode }}
{% endif %}

{% if php_cli_odbc_birdstep_max_links is defined %}
birdstep.max_links = {{ php_cli_odbc_birdstep_max_links }}
{% endif %}

[Interbase]
; Allow or prevent persistent links.
{% if php_cli_ibase_allow_persistent is defined %}
ibase.allow_persistent = {{ php_cli_ibase_allow_persistent }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
{% if php_cli_ibase_max_persistent is defined %}
ibase.max_persistent = {{ php_cli_ibase_max_persistent }}
{% endif %}

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
{% if php_cli_ibase_max_links is defined %}
ibase.max_links = {{ php_cli_ibase_max_links }}
{% endif %}

; Default database name for ibase_connect().
{% if php_cli_ibase_default_db is defined %}
ibase.default_db = {{ php_cli_ibase_default_db }}
{% endif %}

; Default username for ibase_connect().
{% if php_cli_ibase_default_user is defined %}
ibase.default_user = {{ php_cli_ibase_default_user }}
{% endif %}

; Default password for ibase_connect().
{% if php_cli_ibase_default_password is defined %}
ibase.default_password = {{ php_cli_ibase_default_password }}
{% endif %}

; Default charset for ibase_connect().
{% if php_cli_ibase_default_charset is defined %}
ibase.default_charset = {{ php_cli_ibase_default_charset }}
{% endif %}

; Default timestamp format.
{% if php_cli_ibase_timestampformat is defined %}
ibase.timestampformat = {{ php_cli_ibase_timestampformat }}
{% endif %}

; Default date format.
{% if php_cli_ibase_dateformat is defined %}
ibase.dateformat = {{ php_cli_ibase_dateformat }}
{% endif %}

; Default time format.
{% if php_cli_ibase_timeformat is defined %}
ibase.timeformat = {{ php_cli_ibase_timeformat }}
{% endif %}

[MySQL]
; Allow accessing, from PHP's perspective, local files with LOAD DATA statements
; http://php.net/mysql.allow_local_infile
{% if php_cli_mysql_allow_local_infile is defined %}
mysql.allow_local_infile = {{ php_cli_mysql_allow_local_infile }}
{% endif %}

; Allow or prevent persistent links.
; http://php.net/mysql.allow-persistent
{% if php_cli_mysql_allow_persistent is defined %}
mysql.allow_persistent = {{ php_cli_mysql_allow_persistent }}
{% endif %}

; If mysqlnd is used: Number of cache slots for the internal result set cache
; http://php.net/mysql.cache_size
{% if php_cli_mysql_cache_size is defined %}
mysql.cache_size = {{ php_cli_mysql_cache_size }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/mysql.max-persistent
{% if php_cli_mysql_max_persistent is defined %}
mysql.max_persistent = {{ php_cli_mysql_max_persistent }}
{% endif %}

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/mysql.max-links
{% if php_cli_mysql_max_links is defined %}
mysql.max_links = {{ php_cli_mysql_max_links }}
{% endif %}

; Default port number for mysql_connect().  If unset, mysql_connect() will use
; the $MYSQL_TCP_PORT or the mysql-tcp entry in /etc/services or the
; compile-time value defined MYSQL_PORT (in that order).  Win32 will only look
; at MYSQL_PORT.
; http://php.net/mysql.default-port
{% if php_cli_mysql_default_port is defined %}
mysql.default_port = {{ php_cli_mysql_default_port }}
{% endif %}

; Default socket name for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/mysql.default-socket
{% if php_cli_mysql_default_socket is defined %}
mysql.default_socket = {{ php_cli_mysql_default_socket }}
{% endif %}

; Default host for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysql.default-host
{% if php_cli_mysql_default_host is defined %}
mysql.default_host = {{ php_cli_mysql_default_host }}
{% endif %}

; Default user for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysql.default-user
{% if php_cli_mysql_default_user is defined %}
mysql.default_user = {{ php_cli_mysql_default_user }}
{% endif %}

; Default password for mysql_connect() (doesn't apply in safe mode).
; Note that this is generally a *bad* idea to store passwords in this file.
; *Any* user with PHP access can run 'echo get_cfg_var("mysql.default_password")
; and reveal this password!  And of course, any users with read access to this
; file will be able to reveal the password as well.
; http://php.net/mysql.default-password
{% if php_cli_mysql_default_password is defined %}
mysql.default_password = {{ php_cli_mysql_default_password }}
{% endif %}

; Maximum time (in seconds) for connect timeout. -1 means no limit
; http://php.net/mysql.connect-timeout
{% if php_cli_mysql_connect_timeout is defined %}
mysql.connect_timeout = {{ php_cli_mysql_connect_timeout }}
{% endif %}

; Trace mode. When trace_mode is active (=On), warnings for table/index scans and
; SQL-Errors will be displayed.
; http://php.net/mysql.trace-mode
{% if php_cli_mysql_trace_mode is defined %}
mysql.trace_mode = {{ php_cli_mysql_trace_mode }}
{% endif %}

[MySQLi]

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/mysqli.max-persistent
{% if php_cli_mysqli_max_persistent is defined %}
mysqli.max_persistent = {{ php_cli_mysqli_max_persistent }}
{% endif %}

; Allow accessing, from PHP's perspective, local files with LOAD DATA statements
; http://php.net/mysqli.allow_local_infile
{% if php_cli_mysqli_allow_local_infile is defined %}
mysqli.allow_local_infile = {{ php_cli_mysqli_allow_local_infile }}
{% endif %}

; Allow or prevent persistent links.
; http://php.net/mysqli.allow-persistent
{% if php_cli_mysqli_allow_persistent is defined %}
mysqli.allow_persistent = {{ php_cli_mysqli_allow_persistent }}
{% endif %}

; Maximum number of links.  -1 means no limit.
; http://php.net/mysqli.max-links
{% if php_cli_mysqli_max_links is defined %}
mysqli.max_links = {{ php_cli_mysqli_max_links }}
{% endif %}

; If mysqlnd is used: Number of cache slots for the internal result set cache
; http://php.net/mysqli.cache_size
{% if php_cli_mysqli_cache_size is defined %}
mysqli.cache_size = {{ php_cli_mysqli_cache_size }}
{% endif %}

; Default port number for mysqli_connect().  If unset, mysqli_connect() will use
; the $MYSQL_TCP_PORT or the mysql-tcp entry in /etc/services or the
; compile-time value defined MYSQL_PORT (in that order).  Win32 will only look
; at MYSQL_PORT.
; http://php.net/mysqli.default-port
{% if php_cli_mysqli_default_port is defined %}
mysqli.default_port = {{ php_cli_mysqli_default_port }}
{% endif %}

; Default socket name for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/mysqli.default-socket
{% if php_cli_mysqli_default_socket is defined %}
mysqli.default_socket = {{ php_cli_mysqli_default_socket }}
{% endif %}

; Default host for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysqli.default-host
{% if php_cli_mysqli_default_host is defined %}
mysqli.default_host = {{ php_cli_mysqli_default_host }}
{% endif %}

; Default user for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysqli.default-user
{% if php_cli_mysqli_default_user is defined %}
mysqli.default_user = {{ php_cli_mysqli_default_user }}
{% endif %}

; Default password for mysqli_connect() (doesn't apply in safe mode).
; Note that this is generally a *bad* idea to store passwords in this file.
; *Any* user with PHP access can run 'echo get_cfg_var("mysqli.default_pw")
; and reveal this password!  And of course, any users with read access to this
; file will be able to reveal the password as well.
; http://php.net/mysqli.default-pw
{% if php_cli_mysqli_default_pw is defined %}
mysqli.default_pw = {{ php_cli_mysqli_default_pw }}
{% endif %}

; Allow or prevent reconnect
{% if php_cli_mysqli_reconnect is defined %}
mysqli.reconnect = {{ php_cli_mysqli_reconnect }}
{% endif %}

[mysqlnd]
; Enable / Disable collection of general statistics by mysqlnd which can be
; used to tune and monitor MySQL operations.
; http://php.net/mysqlnd.collect_statistics
{% if php_cli_mysqlnd_collect_statistics is defined %}
mysqlnd.collect_statistics = {{ php_cli_mysqlnd_collect_statistics }}
{% endif %}

; Enable / Disable collection of memory usage statistics by mysqlnd which can be
; used to tune and monitor MySQL operations.
; http://php.net/mysqlnd.collect_memory_statistics
{% if php_cli_mysqlnd_collect_memory_statistics is defined %}
mysqlnd.collect_memory_statistics = {{ php_cli_mysqlnd_collect_memory_statistics }}
{% endif %}

; Size of a pre-allocated buffer used when sending commands to MySQL in bytes.
; http://php.net/mysqlnd.net_cmd_buffer_size
{% if php_cli_mysqlnd_net_cmd_buffer_size is defined %}
mysqlnd.net_cmd_buffer_size = {{ php_cli_mysqlnd_net_cmd_buffer_size }}
{% endif %}

; Size of a pre-allocated buffer used for reading data sent by the server in
; bytes.
; http://php.net/mysqlnd.net_read_buffer_size
{% if php_cli_mysqlnd_net_read_buffer_size is defined %}
mysqlnd.net_read_buffer_size = {{ php_cli_mysqlnd_net_read_buffer_size }}
{% endif %}

[OCI8]

; Connection: Enables privileged connections using external
; credentials (OCI_SYSOPER, OCI_SYSDBA)
; http://php.net/oci8.privileged-connect
{% if php_cli_oci8_privileged_connect is defined %}
oci8.privileged_connect = {{ php_cli_oci8_privileged_connect }}
{% endif %}

; Connection: The maximum number of persistent OCI8 connections per
; process. Using -1 means no limit.
; http://php.net/oci8.max-persistent
{% if php_cli_oci8_max_persistent is defined %}
oci8.max_persistent = {{ php_cli_oci8_max_persistent }}
{% endif %}

; Connection: The maximum number of seconds a process is allowed to
; maintain an idle persistent connection. Using -1 means idle
; persistent connections will be maintained forever.
; http://php.net/oci8.persistent-timeout
{% if php_cli_oci8_persistent_timeout is defined %}
oci8.persistent_timeout = {{ php_cli_oci8_persistent_timeout }}
{% endif %}

; Connection: The number of seconds that must pass before issuing a
; ping during oci_pconnect() to check the connection validity. When
; set to 0, each oci_pconnect() will cause a ping. Using -1 disables
; pings completely.
; http://php.net/oci8.ping-interval
{% if php_cli_oci8_ping_interval is defined %}
oci8.ping_interval = {{ php_cli_oci8_ping_interval }}
{% endif %}

; Connection: Set this to a user chosen connection class to be used
; for all pooled server requests with Oracle 11g Database Resident
; Connection Pooling (DRCP).  To use DRCP, this value should be set to
; the same string for all web servers running the same application,
; the database pool must be configured, and the connection string must
; specify to use a pooled server.
{% if php_cli_oci8_connection_class is defined %}
oci8.connection_class = {{ php_cli_oci8_connection_class }}
{% endif %}

; High Availability: Using On lets PHP receive Fast Application
; Notification (FAN) events generated when a database node fails. The
; database must also be configured to post FAN events.
{% if php_cli_oci8_events is defined %}
oci8.events = {{ php_cli_oci8_events }}
{% endif %}

; Tuning: This option enables statement caching, and specifies how
; many statements to cache. Using 0 disables statement caching.
; http://php.net/oci8.statement-cache-size
{% if php_cli_oci8_statement_cache_size is defined %}
oci8.statement_cache_size = {{ php_cli_oci8_statement_cache_size }}
{% endif %}

; Tuning: Enables statement prefetching and sets the default number of
; rows that will be fetched automatically after statement execution.
; http://php.net/oci8.default-prefetch
{% if php_cli_oci8_default_prefetch is defined %}
oci8.default_prefetch = {{ php_cli_oci8_default_prefetch }}
{% endif %}

; Compatibility. Using On means oci_close() will not close
; oci_connect() and oci_new_connect() connections.
; http://php.net/oci8.old-oci-close-semantics
{% if php_cli_oci8_old_oci_close_semantics is defined %}
oci8.old_oci_close_semantics = {{ php_cli_oci8_old_oci_close_semantics }}
{% endif %}

[PostgreSQL]
; Allow or prevent persistent links.
; http://php.net/pgsql.allow-persistent
{% if php_cli_pgsql_allow_persistent is defined %}
pgsql.allow_persistent = {{ php_cli_pgsql_allow_persistent }}
{% endif %}

; Detect broken persistent links always with pg_pconnect().
; Auto reset feature requires a little overheads.
; http://php.net/pgsql.auto-reset-persistent
{% if php_cli_pgsql_auto_reset_persistent is defined %}
pgsql.auto_reset_persistent = {{ php_cli_pgsql_auto_reset_persistent }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/pgsql.max-persistent
{% if php_cli_pgsql_max_persistent is defined %}
pgsql.max_persistent = {{ php_cli_pgsql_max_persistent }}
{% endif %}

; Maximum number of links (persistent+non persistent).  -1 means no limit.
; http://php.net/pgsql.max-links
{% if php_cli_pgsql_max_links is defined %}
pgsql.max_links = {{ php_cli_pgsql_max_links }}
{% endif %}

; Ignore PostgreSQL backends Notice message or not.
; Notice message logging require a little overheads.
; http://php.net/pgsql.ignore-notice
{% if php_cli_pgsql_ignore_notice is defined %}
pgsql.ignore_notice = {{ php_cli_pgsql_ignore_notice }}
{% endif %}

; Log PostgreSQL backends Notice message or not.
; Unless pgsql.ignore_notice=0, module cannot log notice message.
; http://php.net/pgsql.log-notice
{% if php_cli_pgsql_log_notice is defined %}
pgsql.log_notice = {{ php_cli_pgsql_log_notice }}
{% endif %}

[Sybase-CT]
; Allow or prevent persistent links.
; http://php.net/sybct.allow-persistent
{% if php_cli_sybct_allow_persistent is defined %}
sybct.allow_persistent = {{ php_cli_sybct_allow_persistent }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/sybct.max-persistent
{% if php_cli_sybct_max_persistent is defined %}
sybct.max_persistent = {{ php_cli_sybct_max_persistent }}
{% endif %}

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/sybct.max-links
{% if php_cli_sybct_max_links is defined %}
sybct.max_links = {{ php_cli_sybct_max_links }}
{% endif %}

; Minimum server message severity to display.
; http://php.net/sybct.min-server-severity
{% if php_cli_sybct_min_server_severity is defined %}
sybct.min_server_severity = {{ php_cli_sybct_min_server_severity }}
{% endif %}

; Minimum client message severity to display.
; http://php.net/sybct.min-client-severity
{% if php_cli_sybct_min_client_severity is defined %}
sybct.min_client_severity = {{ php_cli_sybct_min_client_severity }}
{% endif %}

; Set per-context timeout
; http://php.net/sybct.timeout
{% if php_cli_sybct_timeout is defined %}
sybct.timeout = {{ php_cli_sybct_timeout }}
{% endif %}

{% if php_cli_sybct_packet_size is defined %}
sybct.packet_size = {{ php_cli_sybct_packet_size }}
{% endif %}

; The maximum time in seconds to wait for a connection attempt to succeed before returning failure.
; Default: one minute
{% if php_cli_sybct_login_timeout is defined %}
sybct.login_timeout = {{ php_cli_sybct_login_timeout }}
{% endif %}

; The name of the host you claim to be connecting from, for display by sp_who.
; Default: none
{% if php_cli_sybct_hostname is defined %}
sybct.hostname = {{ php_cli_sybct_hostname }}
{% endif %}

; Allows you to define how often deadlocks are to be retried. -1 means "forever".
; Default: 0
{% if php_cli_sybct_deadlock_retry_count is defined %}
sybct.deadlock_retry_count = {{ php_cli_sybct_deadlock_retry_count }}
{% endif %}

[bcmath]
; Number of decimal digits for all bcmath functions.
; http://php.net/bcmath.scale
{% if php_cli_bcmath_scale is defined %}
bcmath.scale = {{ php_cli_bcmath_scale }}
{% endif %}

[browscap]
; http://php.net/browscap
{% if php_cli_browscap is defined %}
browscap = {{ php_cli_browscap }}
{% endif %}

[Session]
; Handler used to store/retrieve data.
; http://php.net/session.save-handler
{% if php_cli_session_save_handler is defined %}
session.save_handler = {{ php_cli_session_save_handler }}
{% endif %}

; Argument passed to save_handler.  In the case of files, this is the path
; where data files are stored. Note: Windows users have to change this
; variable in order to use PHP's session functions.
;
; The path can be defined as:
;
;     session.save_path = "N;/path"
;
; where N is an integer.  Instead of storing all the session files in
; /path, what this will do is use subdirectories N-levels deep, and
; store the session data in those directories.  This is useful if you
; or your OS have problems with lots of files in one directory, and is
; a more efficient layout for servers that handle lots of sessions.
;
; NOTE 1: PHP will not create this directory structure automatically.
;         You can use the script in the ext/session dir for that purpose.
; NOTE 2: See the section on garbage collection below if you choose to
;         use subdirectories for session storage
;
; The file storage module creates files using mode 600 by default.
; You can change that by using
;
;     session.save_path = "N;MODE;/path"
;
; where MODE is the octal representation of the mode. Note that this
; does not overwrite the process's umask.
; http://php.net/session.save-path
{% if php_cli_session_save_path is defined %}
session.save_path = {{ php_cli_session_save_path }}
{% endif %}

; Whether to use strict session mode.
; Strict session mode does not accept uninitialized session ID and regenerate
; session ID if browser sends uninitialized session ID. Strict mode protects
; applications from session fixation via session adoption vulnerability. It is
; disabled by default for maximum compatibility, but enabling it is encouraged.
; https://wiki.php.net/rfc/strict_sessions
{% if php_cli_session_use_strict_mode is defined %}
session.use_strict_mode = {{ php_cli_session_use_strict_mode }}
{% endif %}

; Whether to use cookies.
; http://php.net/session.use-cookies
{% if php_cli_session_use_cookies is defined %}
session.use_cookies = {{ php_cli_session_use_cookies }}
{% endif %}

; http://php.net/session.cookie-secure
{% if php_cli_session_cookie_secure is defined %}
session.cookie_secure = {{ php_cli_session_cookie_secure }}
{% endif %}

; This option forces PHP to fetch and use a cookie for storing and maintaining
; the session id. We encourage this operation as it's very helpful in combating
; session hijacking when not specifying and managing your own session id. It is
; not the end all be all of session hijacking defense, but it's a good start.
; http://php.net/session.use-only-cookies
{% if php_cli_session_use_only_cookies is defined %}
session.use_only_cookies = {{ php_cli_session_use_only_cookies }}
{% endif %}

; Name of the session (used as cookie name).
; http://php.net/session.name
{% if php_cli_session_name is defined %}
session.name = {{ php_cli_session_name }}
{% endif %}

; Initialize session on request startup.
; http://php.net/session.auto-start
{% if php_cli_session_auto_start is defined %}
session.auto_start = {{ php_cli_session_auto_start }}
{% endif %}

; Lifetime in seconds of cookie or, if 0, until browser is restarted.
; http://php.net/session.cookie-lifetime
{% if php_cli_session_cookie_lifetime is defined %}
session.cookie_lifetime = {{ php_cli_session_cookie_lifetime }}
{% endif %}

; The path for which the cookie is valid.
; http://php.net/session.cookie-path
{% if php_cli_session_cookie_path is defined %}
session.cookie_path = {{ php_cli_session_cookie_path }}
{% endif %}

; The domain for which the cookie is valid.
; http://php.net/session.cookie-domain
{% if php_cli_session_cookie_domain is defined %}
session.cookie_domain = {{ php_cli_session_cookie_domain }}
{% endif %}

; Whether or not to add the httpOnly flag to the cookie, which makes it inaccessible to browser scripting languages such as JavaScript.
; http://php.net/session.cookie-httponly
{% if php_cli_session_cookie_httponly is defined %}
session.cookie_httponly = {{ php_cli_session_cookie_httponly }}
{% endif %}

; Handler used to serialize data.  php is the standard serializer of PHP.
; http://php.net/session.serialize-handler
{% if php_cli_session_serialize_handler is defined %}
session.serialize_handler = {{ php_cli_session_serialize_handler }}
{% endif %}

; Defines the probability that the 'garbage collection' process is started
; on every session initialization. The probability is calculated by using
; gc_probability/gc_divisor. Where session.gc_probability is the numerator
; and gc_divisor is the denominator in the equation. Setting this value to 1
; when the session.gc_divisor value is 100 will give you approximately a 1% chance
; the gc will run on any give request.
; Default Value: 1
; Development Value: 1
; Production Value: 1
; http://php.net/session.gc-probability
{% if php_cli_session_gc_probability is defined %}
session.gc_probability = {{ php_cli_session_gc_probability }}
{% endif %}

; Defines the probability that the 'garbage collection' process is started on every
; session initialization. The probability is calculated by using the following equation:
; gc_probability/gc_divisor. Where session.gc_probability is the numerator and
; session.gc_divisor is the denominator in the equation. Setting this value to 1
; when the session.gc_divisor value is 100 will give you approximately a 1% chance
; the gc will run on any give request. Increasing this value to 1000 will give you
; a 0.1% chance the gc will run on any give request. For high volume production servers,
; this is a more efficient approach.
; Default Value: 100
; Development Value: 1000
; Production Value: 1000
; http://php.net/session.gc-divisor
{% if php_cli_session_gc_divisor is defined %}
session.gc_divisor = {{ php_cli_session_gc_divisor }}
{% endif %}

; After this number of seconds, stored data will be seen as 'garbage' and
; cleaned up by the garbage collection process.
; http://php.net/session.gc-maxlifetime
{% if php_cli_session_gc_maxlifetime is defined %}
session.gc_maxlifetime = {{ php_cli_session_gc_maxlifetime }}
{% endif %}

; NOTE: If you are using the subdirectory option for storing session files
;       (see session.save_path above), then garbage collection does *not*
;       happen automatically.  You will need to do your own garbage
;       collection through a shell script, cron entry, or some other method.
;       For example, the following script would is the equivalent of
;       setting session.gc_maxlifetime to 1440 (1440 seconds = 24 minutes):
;          find /path/to/sessions -cmin +24 -type f | xargs rm

; Check HTTP Referer to invalidate externally stored URLs containing ids.
; HTTP_REFERER has to contain this substring for the session to be
; considered as valid.
; http://php.net/session.referer-check
{% if php_cli_session_referer_check is defined %}
session.referer_check = {{ php_cli_session_referer_check }}
{% endif %}

; How many bytes to read from the file.
; http://php.net/session.entropy-length
{% if php_cli_session_entropy_length is defined %}
session.entropy_length = {{ php_cli_session_entropy_length }}
{% endif %}

; Specified here to create the session id.
; http://php.net/session.entropy-file
; Defaults to /dev/urandom
; On systems that don't have /dev/urandom but do have /dev/arandom, this will default to /dev/arandom
; If neither are found at compile time, the default is no entropy file.
; On windows, setting the entropy_length setting will activate the
; Windows random source (using the CryptoAPI)
{% if php_cli_session_entropy_file is defined %}
session.entropy_file = {{ php_cli_session_entropy_file }}
{% endif %}

; Set to {nocache,private,public,} to determine HTTP caching aspects
; or leave this empty to avoid sending anti-caching headers.
; http://php.net/session.cache-limiter
{% if php_cli_session_cache_limiter is defined %}
session.cache_limiter = {{ php_cli_session_cache_limiter }}
{% endif %}

; Document expires after n minutes.
; http://php.net/session.cache-expire
{% if php_cli_session_cache_expire is defined %}
session.cache_expire = {{ php_cli_session_cache_expire }}
{% endif %}

; trans sid support is disabled by default.
; Use of trans sid may risk your users security.
; Use this option with caution.
; - User may send URL contains active session ID
;   to other person via. email/irc/etc.
; - URL that contains active session ID may be stored
;   in publicly accessible computer.
; - User may access your site with the same session ID
;   always using URL stored in browser's history or bookmarks.
; http://php.net/session.use-trans-sid
{% if php_cli_session_use_trans_sid is defined %}
session.use_trans_sid = {{ php_cli_session_use_trans_sid }}
{% endif %}

; Select a hash function for use in generating session ids.
; Possible Values
;   0  (MD5 128 bits)
;   1  (SHA-1 160 bits)
; This option may also be set to the name of any hash function supported by
; the hash extension. A list of available hashes is returned by the hash_algos()
; function.
; http://php.net/session.hash-function
{% if php_cli_session_hash_function is defined %}
session.hash_function = {{ php_cli_session_hash_function }}
{% endif %}

; Define how many bits are stored in each character when converting
; the binary hash data to something readable.
; Possible values:
;   4  (4 bits: 0-9, a-f)
;   5  (5 bits: 0-9, a-v)
;   6  (6 bits: 0-9, a-z, A-Z, "-", ",")
; Default Value: 4
; Development Value: 5
; Production Value: 5
; http://php.net/session.hash-bits-per-character
{% if php_cli_session_hash_bits_per_character is defined %}
session.hash_bits_per_character = {{ php_cli_session_hash_bits_per_character }}
{% endif %}

; The URL rewriter will look for URLs in a defined set of HTML tags.
; form/fieldset are special; if you include them here, the rewriter will
; add a hidden <input> field with the info which is otherwise appended
; to URLs.  If you want XHTML conformity, remove the form entry.
; Note that all valid entries require a "=", even if no value follows.
; Default Value: "a=href,area=href,frame=src,form=,fieldset="
; Development Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
; Production Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
; http://php.net/url-rewriter.tags
{% if php_cli_session_url_rewriter_tags is defined %}
url_rewriter.tags = "{{ php_cli_session_url_rewriter_tags }}"
{% endif %}

; Enable upload progress tracking in $_SESSION
; Default Value: On
; Development Value: On
; Production Value: On
; http://php.net/session.upload-progress.enabled
{% if php_cli_session_upload_progress_enabled is defined %}
session.upload_progress.enabled = {{ php_cli_session_upload_progress_enabled }}
{% endif %}

; Cleanup the progress information as soon as all POST data has been read
; (i.e. upload completed).
; Default Value: On
; Development Value: On
; Production Value: On
; http://php.net/session.upload-progress.cleanup
{% if php_cli_session_upload_progress_cleanup is defined %}
session.upload_progress.cleanup = {{ php_cli_session_upload_progress_cleanup }}
{% endif %}

; A prefix used for the upload progress key in $_SESSION
; Default Value: "upload_progress_"
; Development Value: "upload_progress_"
; Production Value: "upload_progress_"
; http://php.net/session.upload-progress.prefix
{% if php_cli_session_upload_progress_prefix is defined %}
session.upload_progress.prefix = {{ php_cli_session_upload_progress_prefix }}
{% endif %}

; The index name (concatenated with the prefix) in $_SESSION
; containing the upload progress information
; Default Value: "PHP_SESSION_UPLOAD_PROGRESS"
; Development Value: "PHP_SESSION_UPLOAD_PROGRESS"
; Production Value: "PHP_SESSION_UPLOAD_PROGRESS"
; http://php.net/session.upload-progress.name
{% if php_cli_session_upload_progress_name is defined %}
session.upload_progress.name = {{ php_cli_session_upload_progress_name }}
{% endif %}

; How frequently the upload progress should be updated.
; Given either in percentages (per-file), or in bytes
; Default Value: "1%"
; Development Value: "1%"
; Production Value: "1%"
; http://php.net/session.upload-progress.freq
{% if php_cli_session_upload_progress_freq is defined %}
session.upload_progress.freq = {{ php_cli_session_upload_progress_freq }}
{% endif %}

; The minimum delay between updates, in seconds
; Default Value: 1
; Development Value: 1
; Production Value: 1
; http://php.net/session.upload-progress.min-freq
{% if php_cli_session_upload_progress_min_freq is defined %}
session.upload_progress.min_freq = {{ php_cli_session_upload_progress_min_freq }}
{% endif %}

[MSSQL]
; Allow or prevent persistent links.
{% if php_cli_mssql_allow_persistent is defined %}
mssql.allow_persistent = {{ php_cli_mssql_allow_persistent }}
{% endif %}

; Maximum number of persistent links.  -1 means no limit.
{% if php_cli_mssql_max_persistent is defined %}
mssql.max_persistent = {{ php_cli_mssql_max_persistent }}
{% endif %}

; Maximum number of links (persistent+non persistent).  -1 means no limit.
{% if php_cli_mssql_max_links is defined %}
mssql.max_links = {{ php_cli_mssql_max_links }}
{% endif %}

; Minimum error severity to display.
{% if php_cli_mssql_min_error_severity is defined %}
mssql.min_error_severity = {{ php_cli_mssql_min_error_severity }}
{% endif %}

; Minimum message severity to display.
{% if php_cli_mssql_min_message_severity is defined %}
mssql.min_message_severity = {{ php_cli_mssql_min_message_severity }}
{% endif %}

; Compatibility mode with old versions of PHP 3.0.
{% if php_cli_mssql_compatibility_mode is defined %}
mssql.compatibility_mode = {{ php_cli_mssql_compatibility_mode }}
{% endif %}

; Connect timeout
{% if php_cli_mssql_connect_timeout is defined %}
mssql.connect_timeout = {{ php_cli_mssql_connect_timeout }}
{% endif %}

; Query timeout
{% if php_cli_mssql_timeout is defined %}
mssql.timeout = {{ php_cli_mssql_timeout }}
{% endif %}

; Valid range 0 - 2147483647.  Default = 4096.
{% if php_cli_mssql_textlimit is defined %}
mssql.textlimit = {{ php_cli_mssql_textlimit }}
{% endif %}

; Valid range 0 - 2147483647.  Default = 4096.
{% if php_cli_mssql_textsize is defined %}
mssql.textsize = {{ php_cli_mssql_textsize }}
{% endif %}

; Limits the number of records in each batch.  0 = all records in one batch.
{% if php_cli_mssql_batchsize is defined %}
mssql.batchsize = {{ php_cli_mssql_batchsize }}
{% endif %}

; Specify how datetime and datetim4 columns are returned
; On => Returns data converted to SQL server settings
; Off => Returns values as YYYY-MM-DD hh:mm:ss
{% if php_cli_mssql_datetimeconvert is defined %}
mssql.datetimeconvert = {{ php_cli_mssql_datetimeconvert }}
{% endif %}

; Use NT authentication when connecting to the server
{% if php_cli_mssql_secure_connection is defined %}
mssql.secure_connection = {{ php_cli_mssql_secure_connection }}
{% endif %}

; Specify max number of processes. -1 = library default
; msdlib defaults to 25
; FreeTDS defaults to 4096
{% if php_cli_mssql_max_procs is defined %}
mssql.max_procs = {{ php_cli_mssql_max_procs }}
{% endif %}

; Specify client character set.
; If empty or not set the client charset from freetds.conf is used
; This is only used when compiled with FreeTDS
{% if php_cli_mssql_charset is defined %}
mssql.charset = {{ php_cli_mssql_charset }}
{% endif %}

[Assertion]
; Assert(expr); active by default.
; http://php.net/assert.active
{% if php_cli_assert_active is defined %}
assert.active = {{ php_cli_assert_active }}
{% endif %}

; Issue a PHP warning for each failed assertion.
; http://php.net/assert.warning
{% if php_cli_assert_warning is defined %}
assert.warning = {{ php_cli_assert_warning }}
{% endif %}

; Don't bail out by default.
; http://php.net/assert.bail
{% if php_cli_assert_bail is defined %}
assert.bail = {{ php_cli_assert_bail }}
{% endif %}

; User-function to be called if an assertion fails.
; http://php.net/assert.callback
{% if php_cli_assert_callback is defined %}
assert.callback = {{ php_cli_assert_callback }}
{% endif %}

; Eval the expression with current error_reporting().  Set to true if you want
; error_reporting(0) around the eval().
; http://php.net/assert.quiet-eval
{% if php_cli_assert_quiet_eval is defined %}
assert.quiet_eval = {{ php_cli_assert_quiet_eval }}
{% endif %}

[COM]
; path to a file containing GUIDs, IIDs or filenames of files with TypeLibs
; http://php.net/com.typelib-file
{% if php_cli_com_typelib_file is defined %}
com.typelib_file = {{ php_cli_com_typelib_file }}
{% endif %}

; allow Distributed-COM calls
; http://php.net/com.allow-dcom
{% if php_cli_com_allow_dcom is defined %}
com.allow_dcom = {{ php_cli_com_allow_dcom }}
{% endif %}

; autoregister constants of a components typlib on com_load()
; http://php.net/com.autoregister-typelib
{% if php_cli_com_autoregister_typelib is defined %}
com.autoregister_typelib = {{ php_cli_com_autoregister_typelib }}
{% endif %}

; register constants casesensitive
; http://php.net/com.autoregister-casesensitive
{% if php_cli_com_autoregister_casesensitive is defined %}
com.autoregister_casesensitive = {{ php_cli_com_autoregister_casesensitive }}
{% endif %}

; show warnings on duplicate constant registrations
; http://php.net/com.autoregister-verbose
{% if php_cli_com_autoregister_verbose is defined %}
com.autoregister_verbose = {{ php_cli_com_autoregister_verbose }}
{% endif %}

; The default character set code-page to use when passing strings to and from COM objects.
; Default: system ANSI code page
{% if php_cli_com_code_page is defined %}
com.code_page = {{ php_cli_com_code_page }}
{% endif %}

[mbstring]
; language for internal character representation.
; http://php.net/mbstring.language
{% if php_cli_mbstring_language is defined %}
mbstring.language = {{ php_cli_mbstring_language }}
{% endif %}

; internal/script encoding.
; Some encoding cannot work as internal encoding.
; (e.g. SJIS, BIG5, ISO-2022-*)
; http://php.net/mbstring.internal-encoding
{% if php_cli_mbstring_internal_encoding is defined %}
mbstring.internal_encoding = {{ php_cli_mbstring_internal_encoding }}
{% endif %}

; http input encoding.
; http://php.net/mbstring.http-input
{% if php_cli_mbstring_http_input is defined %}
mbstring.http_input = {{ php_cli_mbstring_http_input }}
{% endif %}

; http output encoding. mb_output_handler must be
; registered as output buffer to function
; http://php.net/mbstring.http-output
{% if php_cli_mbstring_http_output is defined %}
mbstring.http_output = {{ php_cli_mbstring_http_output }}
{% endif %}

; enable automatic encoding translation according to
; mbstring.internal_encoding setting. Input chars are
; converted to internal encoding by setting this to On.
; Note: Do _not_ use automatic encoding translation for
;       portable libs/applications.
; http://php.net/mbstring.encoding-translation
{% if php_cli_mbstring_encoding_translation is defined %}
mbstring.encoding_translation = {{ php_cli_mbstring_encoding_translation }}
{% endif %}

; automatic encoding detection order.
; auto means
; http://php.net/mbstring.detect-order
{% if php_cli_mbstring_detect_order is defined %}
mbstring.detect_order = {{ php_cli_mbstring_detect_order }}
{% endif %}

; substitute_character used when character cannot be converted
; one from another
; http://php.net/mbstring.substitute-character
{% if php_cli_mbstring_substitute_character is defined %}
mbstring.substitute_character = {{ php_cli_mbstring_substitute_character }}
{% endif %}

; overload(replace) single byte functions by mbstring functions.
; mail(), ereg(), etc are overloaded by mb_send_mail(), mb_ereg(),
; etc. Possible values are 0,1,2,4 or combination of them.
; For example, 7 for overload everything.
; 0: No overload
; 1: Overload mail() function
; 2: Overload str*() functions
; 4: Overload ereg*() functions
; http://php.net/mbstring.func-overload
{% if php_cli_mbstring_func_overload is defined %}
mbstring.func_overload = {{ php_cli_mbstring_func_overload }}
{% endif %}

; enable strict encoding detection.
{% if php_cli_mbstring_strict_detection is defined %}
mbstring.strict_detection = {{ php_cli_mbstring_strict_detection }}
{% endif %}

; This directive specifies the regex pattern of content types for which mb_output_handler()
; is activated.
; Default: mbstring.http_output_conv_mimetype=^(text/|application/xhtml\+xml)
{% if php_cli_mbstring_http_output_conv_mimetype is defined %}
mbstring.http_output_conv_mimetype = {{ php_cli_mbstring_http_output_conv_mimetype }}
{% endif %}

[gd]
; Tell the jpeg decode to ignore warnings and try to create
; a gd image. The warning will then be displayed as notices
; disabled by default
; http://php.net/gd.jpeg-ignore-warning
{% if php_cli_gd_jpeg_ignore_warning is defined %}
gd.jpeg_ignore_warning = {{ php_cli_gd_jpeg_ignore_warning }}
{% endif %}

[exif]
; Exif UNICODE user comments are handled as UCS-2BE/UCS-2LE and JIS as JIS.
; With mbstring support this will automatically be converted into the encoding
; given by corresponding encode setting. When empty mbstring.internal_encoding
; is used. For the decode settings you can distinguish between motorola and
; intel byte order. A decode setting cannot be empty.
; http://php.net/exif.encode-unicode
{% if php_cli_exif_encode_unicode is defined %}
exif.encode_unicode = {{ php_cli_exif_encode_unicode }}
{% endif %}

; http://php.net/exif.decode-unicode-motorola
{% if php_cli_exif_decode_unicode_motorola is defined %}
exif.decode_unicode_motorola = {{ php_cli_exif_decode_unicode_motorola }}
{% endif %}

; http://php.net/exif.decode-unicode-intel
{% if php_cli_exif_decode_unicode_intel is defined %}
exif.decode_unicode_intel = {{ php_cli_exif_decode_unicode_intel }}
{% endif %}

; http://php.net/exif.encode-jis
{% if php_cli_exif_encode_jis is defined %}
exif.encode_jis= {{ php_cli_exif_encode_jis }}
{% endif %}

; http://php.net/exif.decode-jis-motorola
{% if php_cli_exif_decode_jis_motorola is defined %}
exif.decode_jis_motorola = {{ php_cli_exif_decode_jis_motorola }}
{% endif %}

; http://php.net/exif.decode-jis-intel
{% if php_cli_exif_decode_jis_intel is defined %}
exif.decode_jis_intel = {{ php_cli_exif_decode_jis_intel }}
{% endif %}

[Tidy]
; The path to a default tidy configuration file to use when using tidy
; http://php.net/tidy.default-config
{% if php_cli_tidy_default_config is defined %}
tidy.default_config = {{ php_cli_tidy_default_config }}
{% endif %}

; Should tidy clean and repair output automatically?
; WARNING: Do not use this option if you are generating non-html content
; such as dynamic images
; http://php.net/tidy.clean-output
{% if php_cli_tidy_clean_output is defined %}
tidy.clean_output = {{ php_cli_tidy_clean_output }}
{% endif %}

[soap]
; Enables or disables WSDL caching feature.
; http://php.net/soap.wsdl-cache-enabled
{% if php_cli_soap_wsdl_cache_enabled is defined %}
soap.wsdl_cache_enabled = {{ php_cli_soap_wsdl_cache_enabled }}
{% endif %}

; Sets the directory name where SOAP extension will put cache files.
; http://php.net/soap.wsdl-cache-dir
{% if php_cli_soap_wsdl_cache_dir is defined %}
soap.wsdl_cache_dir = {{ php_cli_soap_wsdl_cache_dir }}
{% endif %}

; (time to live) Sets the number of second while cached file will be used
; instead of original one.
; http://php.net/soap.wsdl-cache-ttl
{% if php_cli_soap_wsdl_cache_ttl is defined %}
soap.wsdl_cache_ttl = {{ php_cli_soap_wsdl_cache_ttl }}
{% endif %}

; Sets the size of the cache limit. (Max. number of WSDL files to cache)
{% if php_cli_soap_wsdl_cache_limit is defined %}
soap.wsdl_cache_limit = {{ php_cli_soap_wsdl_cache_limit }}
{% endif %}

[sysvshm]
; A default size of the shared memory segment
{% if php_cli_sysvshm_init_mem is defined %}
sysvshm.init_mem = {{ php_cli_sysvshm_init_mem }}
{% endif %}

[ldap]
; Sets the maximum number of open links or -1 for unlimited.
{% if php_cli_ldap_max_links is defined %}
ldap.max_links = {{ php_cli_ldap_max_links }}
{% endif %}

[mcrypt]
; For more information about mcrypt settings see http://php.net/mcrypt-module-open

; Directory where to load mcrypt algorithms
; Default: Compiled in into libmcrypt (usually /usr/local/lib/libmcrypt)
{% if php_cli_mcrypt_algorithms_dir is defined %}
mcrypt.algorithms_dir = {{ php_cli_mcrypt_algorithms_dir }}
{% endif %}

; Directory where to load mcrypt modes
; Default: Compiled in into libmcrypt (usually /usr/local/lib/libmcrypt)
{% if php_cli_mcrypt_modes_dir is defined %}
mcrypt.modes_dir = {{ php_cli_mcrypt_modes_dir }}
{% endif %}

[dba]
{% if php_cli_dba_default_handler is defined %}
dba.default_handler = {{ php_cli_dba_default_handler }}
{% endif %}

[opcache]
; Determines if Zend OPCache is enabled
{% if php_cli_opcache_enable is defined %}
opcache.enable = {{ php_cli_opcache_enable }}
{% endif %}

; Determines if Zend OPCache is enabled for the CLI version of PHP
{% if php_cli_opcache_enable_cli is defined %}
opcache.enable_cli = {{ php_cli_opcache_enable_cli }}
{% endif %}

; The OPcache shared memory storage size.
{% if php_cli_opcache_memory_consumption is defined %}
opcache.memory_consumption = {{ php_cli_opcache_memory_consumption }}
{% endif %}

; The amount of memory for interned strings in Mbytes.
{% if php_cli_opcache_interned_strings_buffer is defined %}
opcache.interned_strings_buffer = {{ php_cli_opcache_interned_strings_buffer }}
{% endif %}

; The maximum number of keys (scripts) in the OPcache hash table.
; Only numbers between 200 and 100000 are allowed.
{% if php_cli_opcache_max_accelerated_files is defined %}
opcache.max_accelerated_files = {{ php_cli_opcache_max_accelerated_files }}
{% endif %}

; The maximum percentage of "wasted" memory until a restart is scheduled.
{% if php_cli_opcache_max_wasted_percentage is defined %}
opcache.max_wasted_percentage = {{ php_cli_opcache_max_wasted_percentage }}
{% endif %}

; When this directive is enabled, the OPcache appends the current working
; directory to the script key, thus eliminating possible collisions between
; files with the same name (basename). Disabling the directive improves
; performance, but may break existing applications.
{% if php_cli_opcache_use_cwd is defined %}
opcache.use_cwd = {{ php_cli_opcache_use_cwd }}
{% endif %}

; When disabled, you must reset the OPcache manually or restart the
; webserver for changes to the filesystem to take effect.
{% if php_cli_opcache_validate_timestamps is defined %}
opcache.validate_timestamps = {{ php_cli_opcache_validate_timestamps }}
{% endif %}

; How often (in seconds) to check file timestamps for changes to the shared
; memory storage allocation. ("1" means validate once per second, but only
; once per request. "0" means always validate)
{% if php_cli_opcache_revalidate_freq is defined %}
opcache.revalidate_freq = {{ php_cli_opcache_revalidate_freq }}
{% endif %}

; Enables or disables file search in include_path optimization
{% if php_cli_opcache_revalidate_path is defined %}
opcache.revalidate_path = {{ php_cli_opcache_revalidate_path }}
{% endif %}

; If disabled, all PHPDoc comments are dropped from the code to reduce the
; size of the optimized code.
{% if php_cli_opcache_save_comments is defined %}
opcache.save_comments = {{ php_cli_opcache_save_comments }}
{% endif %}

; If disabled, PHPDoc comments are not loaded from SHM, so "Doc Comments"
; may be always stored (save_comments=1), but not loaded by applications
; that don't need them anyway.
{% if php_cli_opcache_load_comments is defined %}
opcache.load_comments = {{ php_cli_opcache_load_comments }}
{% endif %}

; If enabled, a fast shutdown sequence is used for the accelerated code
{% if php_cli_opcache_fast_shutdown is defined %}
opcache.fast_shutdown = {{ php_cli_opcache_fast_shutdown }}
{% endif %}

; Allow file existence override (file_exists, etc.) performance feature.
{% if php_cli_opcache_enable_file_override is defined %}
opcache.enable_file_override = {{ php_cli_opcache_enable_file_override }}
{% endif %}

; A bitmask, where each bit enables or disables the appropriate OPcache
; passes
{% if php_cli_opcache_optimization_level is defined %}
opcache.optimization_level = {{ php_cli_opcache_optimization_level }}
{% endif %}

{% if php_cli_opcache_inherited_hack is defined %}
opcache.inherited_hack = {{ php_cli_opcache_inherited_hack }}
{% endif %}

{% if php_cli_opcache_dups_fix is defined %}
opcache.dups_fix = {{ php_cli_opcache_dups_fix }}
{% endif %}

; The location of the OPcache blacklist file (wildcards allowed).
; Each OPcache blacklist file is a text file that holds the names of files
; that should not be accelerated. The file format is to add each filename
; to a new line. The filename may be a full path or just a file prefix
; (i.e., /var/www/x  blacklists all the files and directories in /var/www
; that start with 'x'). Line starting with a ; are ignored (comments).
{% if php_cli_opcache_blacklist_filename is defined %}
opcache.blacklist_filename = {{ php_cli_opcache_blacklist_filename }}
{% endif %}

; Allows exclusion of large files from being cached. By default all files
; are cached.
{% if php_cli_opcache_max_file_size is defined %}
opcache.max_file_size = {{ php_cli_opcache_max_file_size }}
{% endif %}

; Check the cache checksum each N requests.
; The default value of "0" means that the checks are disabled.
{% if php_cli_opcache_consistency_checks is defined %}
opcache.consistency_checks = {{ php_cli_opcache_consistency_checks }}
{% endif %}

; How long to wait (in seconds) for a scheduled restart to begin if the cache
; is not being accessed.
{% if php_cli_opcache_force_restart_timeout is defined %}
opcache.force_restart_timeout = {{ php_cli_opcache_force_restart_timeout }}
{% endif %}

; OPcache error_log file name. Empty string assumes "stderr".
{% if php_cli_opcache_error_log is defined %}
opcache.error_log = {{ php_cli_opcache_error_log }}
{% endif %}

; All OPcache errors go to the Web server log.
; By default, only fatal errors (level 0) or errors (level 1) are logged.
; You can also enable warnings (level 2), info messages (level 3) or
; debug messages (level 4).
{% if php_cli_opcache_log_verbosity_level is defined %}
opcache.log_verbosity_level = {{ php_cli_opcache_log_verbosity_level }}
{% endif %}

; Preferred Shared Memory back-end. Leave empty and let the system decide.
{% if php_cli_opcache_preferred_memory_model is defined %}
opcache.preferred_memory_model = {{ php_cli_opcache_preferred_memory_model }}
{% endif %}

; Protect the shared memory from unexpected writing during script execution.
; Useful for internal debugging only.
{% if php_cli_opcache_protect_memory is defined %}
opcache.protect_memory = {{ php_cli_opcache_protect_memory }}
{% endif %}

[curl]
; A default value for the CURLOPT_CAINFO option. This is required to be an
; absolute path.
{% if php_cli_curl_cainfo is defined %}
curl.cainfo = {{ php_cli_curl_cainfo }}
{% endif %}

; Local Variables:
; tab-width: 4
; End:
